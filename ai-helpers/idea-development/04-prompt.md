> [!WARNING]
> **CONTEXT DECOUPLING RULE**
> This prompt MUST NOT contain any references to the root `AGENTS.md` file or any files within the `.agents/` directory. The orchestrating agent already has this context loaded.

# Delegation Prompt

[The generated prompt for the orchestrator will be written here by the generator]
